Create the sendData tool by using `make`.

Usage:

* sendData: `./sendData <Interface Name> <HW Address> <Package count>`
* sendData_con: `./sendData_con <Interface Name> <HWAddress> <Package interval in µs>`
